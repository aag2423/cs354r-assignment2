#ifndef Network_h
#define Network_h

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "SDL_net.h"

using namespace std;

class Network {
public:
	Network();
	~Network();
	void server();
	void client(string d, int p);
	TCPsocket sd, csd;
	IPaddress ip, *remoteIP;
	int quit, quit2;
	char buffer[512];
};

#endif
